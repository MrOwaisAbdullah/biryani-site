import React from "react";
import HeroSlider from "../ui/heroSlider";

const Hero = () => {
  return (
    <div className="relative" style={{ zIndex: 0 }}>
      <HeroSlider />
    </div>
  );
};

export default Hero;
